version: '3.8'

services:
  solid-web-components-ui:
    build: .
    ports:
      - "8080:5173"
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev
    environment:
      - NODE_ENV=development